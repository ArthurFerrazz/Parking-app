import 'package:flutter/material.dart';

class AdicionarEstacionamento extends StatelessWidget {
  const AdicionarEstacionamento({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
